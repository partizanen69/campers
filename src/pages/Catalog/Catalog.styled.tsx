import styled from 'styled-components';

export const CatalogStyled = styled.div`
  display: flex;
  padding: 100px 64px 0 64px;
  height: 100vh;
  gap: 32px;

  .sidebar {
    flex: 0 1 auto;
    width: 360px;
  }

  .camper-list {
    flex: 1 1 auto;
  }
`;
