import styled from 'styled-components';

export const CatalogStyled = styled.div`
  display: flex;

  height: 100vh;
  gap: 32px;

  .sidebar {
    flex: 0 1 auto;
    width: 360px;
  }

  .camper-list {
    flex: 1 1 auto;
  }
`;
